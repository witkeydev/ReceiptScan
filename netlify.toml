[build]
  functions = "netlify/functions"

[build.environment]
  # GOOGLE_CLIENT_ID はOAuthのクライアントIDで、設計上ブラウザに公開される
  # 公開値のため index.html に記載しているが Netlify Secrets Scanning が誤検知するので除外
  SECRETS_SCAN_OMIT_KEYS = "GOOGLE_CLIENT_ID,APPLE_CLIENT_ID"

[functions]
  node_bundler = "esbuild"
